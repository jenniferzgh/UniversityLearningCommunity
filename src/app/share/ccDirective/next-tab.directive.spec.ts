import { NextTabDirective } from './next-tab.directive';

describe('NextTabDirective', () => {
  it('should create an instance', () => {
    const directive = new NextTabDirective();
    expect(directive).toBeTruthy();
  });
});
